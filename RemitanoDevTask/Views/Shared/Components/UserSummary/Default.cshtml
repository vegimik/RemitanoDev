@model List<Microsoft.AspNetCore.Identity.IdentityUser>


@if (Model.Count>0)
{
    <li class="nav-item">
        <a class="nav-link">
            <span class="glyphicon glyphicon-info-sign"></span>
            <span id="cart-status">
                @Model[0].UserName
            </span>
        </a>
    </li>
}